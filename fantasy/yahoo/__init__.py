"""Yahoo Fantasy Sports API client: OAuth, resource client, XML parsing,
caching, sync orchestration, and Yahoo<->NHL player identity
reconciliation. See contracts.py for exactly which endpoints/response
shapes are independently verified vs. based on established documentation."""
